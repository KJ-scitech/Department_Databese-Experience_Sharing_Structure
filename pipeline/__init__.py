"""数据管道：把现有表格里的数据拉下来、洗干净、放进数据库。

分工（详见任务拆解.md 第五节）：

    fetch.py      数据拉取      —— 从在线表格/云盘取回原始数据
    contract.py   中间格式约定  —— 两人共同确定，是这一线的接口
    clean.py      清洗与入库    —— 校验、去重、幂等写入 MySQL

顺序上，成员必须先进库，参与记录才有得关联。
两个脚本都可以加 --from-file 跑本地文件，不依赖线上表格，
方便在没有账号/网络的时候先把逻辑写完。

本目录只放代码。拉下来的数据一律落在 data/ 下（已被 .gitignore 拦住），
不要把任何 csv / xlsx / json 数据文件提交进仓库。
"""
