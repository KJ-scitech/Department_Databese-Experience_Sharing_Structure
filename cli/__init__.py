# -*- coding: utf-8 -*-
"""知识库命令行工具（CLI）。

给 QQ 机器人（AstrBot）读写这个库用，顺便也让人能在终端里查东西。
这是需求文档里 K10「接入 QQ 机器人」的落地方式。

━━━ 怎么跑 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    python -m cli --help                      看全部命令
    python -m cli health                      先自检，服务通不通
    python -m cli exp search "设备出故障了咋办"   语义检索（机器人主要用这个）

服务得先起着，CLI 自己不会把它拉起来：

    python -m uvicorn api.main:app --port 8000

━━━ 包里各文件干什么 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    client.py     HTTP 封装、退出码映射   ← 为什么走 HTTP，看这个文件的注释
    main.py       命令注册、输出信封、退出码出口
    exp.py        经验：search / get / list / add / delete
    directory.py  成员 / 系列 / 活动 / 参与记录
    report.py     三个统计报表

**谁负责哪个文件，看组内的《任务分解》文档**（不进仓库）。
原则是：谁做后端哪一块，就负责对应的 CLI 子命令。

CLI 不自己实现任何业务逻辑，只是把服务端已有的接口包一层——
校验规则（409 重复登记、400 外键不存在、404）全部自动继承，
机器人写进去的数据和网页录入的走同一套规则。

━━━ 两条必须知道的规矩 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. **cli/ 只依赖标准库 + requests，一行 `from api import ...` 都不写。**
   原因见 cli/client.py 开头。

2. **退出码是给机器人用的，不是给人看的。**
   0 成功（含「没搜到」）、3 连不上、4 找不到、5 已经做过了、6 被拒绝。
   完整表在 cli/main.py 开头。
"""
