# 知识库 · Python 后端依赖
#
# 安装（国内必须走镜像）：
#   pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

# ── Web 服务 ──────────────────────────────────────────────
fastapi
uvicorn[standard]

# ── 数据库 ────────────────────────────────────────────────
sqlalchemy
pymysql          # SQLAlchemy 连 MySQL 的驱动

# ── 配置 ──────────────────────────────────────────────────
python-dotenv    # 读取 .env

# ── 数据管道线 ────────────────────────────────────────────
requests         # 从在线表格拉数据

# ── 验收测试 ──────────────────────────────────────────────
# 每个组员都要装：做功能的人写自己那部分的测试（见 任务拆解.md）
pytest

# ── 向量检索（版本已实测，不要随意升降）────────────────────
chromadb==1.5.9
sentence-transformers==6.0.1

# 下面几个是间接依赖，锁版本保证各人结果一致
torch==2.14.0
transformers==5.17.0
numpy==2.4.6
