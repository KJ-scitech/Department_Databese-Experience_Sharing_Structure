# 协作规范（CONTRIBUTING）

> 所有参与本仓库的同学（技术组及协作成员）必须遵守。

## 一、红线：绝对不能提交的内容

- **任何实际数据**：数据库文件（`.db`/`.sqlite`）、CSV/Excel 导出、成员名单、报名表、签到表
- **个人敏感信息**：姓名 + 学号 + 手机 + 邮箱 + 班级等组合
- **密钥 / 凭证**：API token、数据库密码、SSH key、`.env`

> 本仓库只放**框架**：表结构（schema）、代码、文档、配置模板。

## 二、敏感信息只能放在这里

**成员信息、联系方式、活动参与记录等所有敏感数据，只能存放在：**

1. **内网服务器的 MySQL 数据库**（部署在内网，不对外）
2. 授权范围内的**内部在线表格**（团队 SeaTable）

**禁止**：出现在本 GitHub 仓库、公开聊天记录、个人网盘、截图。

数据库连接信息（账号/密码）通过服务器环境变量或 secret 管理，不写进代码、不提交。

## 三、可以提交的内容

| 可以 | 说明 |
|------|------|
| `schema/*.sql` | 表结构（关系模式） |
| 代码 | 录入/查询脚本、工具 |
| `docs/*.md` | 需求、交接、说明文档 |
| `.env.example` | 配置模板（**不含真实值**） |

## 四、协作流程

1. **不要直接 push `main`**（main 已设分支保护）
2. 从 `main` 开分支：`feature/xxx` 或 `fix/xxx`
3. 提交 PR，说明：改了什么、为什么、影响范围
4. **由仓库 owner（@MingShuo-S）审核后 merge**
5. 合并后再删除分支

## 五、提交前自查清单

- [ ] 没有 db / csv / xlsx / 名单 / 报名表 / 签到表
- [ ] 没有手机号 / 学号 / 邮箱等个人信息
- [ ] 没有 token / 密码 / key
- [ ] `git status` 里没有意外的数据文件
- [ ] 只改了框架相关文件

## 六、发现误提交怎么办

立即联系 owner，从仓库历史中清除并更换泄露的凭证。
