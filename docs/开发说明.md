# 开发说明

> 开甲书院团学联学术科创部 · 技术组 · 知识库组
>
> 本文件面向**动手写代码的人**：这个库能干什么、怎么跑起来、代码放在哪。
> 仓库门面——「这个系统是什么、数据从哪来、需求在哪」——在根目录的 `README.md`。

## 这个库能干什么

三件事。

### 1. 可检索的经验知识库

把办活动踩过的坑写成一条条文字存起来，然后**用大白话就能搜到**。

普通搜索是比对文字：库里写的是「投影仪不亮」，你搜「设备出故障了咋办」——
一个字都对不上，一条也搜不出来。这个库做的是**按意思搜**，两句话字面上毫无关系，
也能把那条揪出来：

| 你在群里问 | 它能给你捞出来 |
|---|---|
| 「报名的人太少咋办」 | 「可以在班群和部门群二次宣传，或者合并到下一场」 |
| 「设备坏了」 | 「投影仪不亮，先检查 HDMI 线是否松动，备一台笔记本」 |

存储：向量库（Chroma）。非结构化，靠语义相似度召回。

### 2. 活动分工统计

每办一场活动，记一条「**谁** × **哪场活动** × **什么分工**」。记下来之后两个方向都能查：

- **看一场活动**：这场沙龙谁是主讲、谁是执行、谁负责搬设备。
- **看一个人**：某同学这学期参加了哪几场活动，各是什么角色，投入多少小时。

再往上出统计：每个人参与几次、谁**一次都没参与过**（排轮值最该先看这个）、
按学期看活是不是压在少数人身上。

存储：MySQL。结构化，可统计、可出报表。

用途：**换届交接、轮值安排、评优**的依据。

> ⚠️ 这个库**不出五育时长和志愿时长**。需求文档写明了时长不由本系统产出，
> 只统计「谁干了几次、干了什么角色」，是安排工作的参考，不是发放时长的依据。

### 3. 给 QQ 机器人读写

QQBot 组的机器人（AstrBot）能直接读写这个库，群里的样子大概是：

```
同学：@机器人 投影仪不亮咋办
机器人：经验库里有一条相关的 ——
       活动现场投影仪突然不亮，先检查 HDMI 线是否松动，
       备一台笔记本，联系场地管理员拿备用投影。
```

机器人能干四件事：**查经验、写经验、查分工统计、写分工登记**。
入口是我们提供的 [命令行工具（CLI）](#命令行工具cli)——机器人不直接连数据库，
见那一节说明。

一期只在学创部内部跑通，稳定后再考虑开放给全团学联。

## 需求来源

- 需求文档：本仓库的 `docs/需求文档.md`（部长发布）
- 交接文档：本仓库的 `docs/交接文档.md`
- 仓库：https://github.com/KJ-scitech/Department_Databese-Experience_Sharing_Structure

> ⚠️ 本仓库只放**框架**（表结构、代码、文档、配置模板）。
> **任何实际数据、个人信息、密钥都禁止提交。** 详见 `CONTRIBUTING.md`。
>
> `main` 是受保护分支：**不许直接 push**，改动一律开分支 + 提 PR，由 owner 审核后 merge。

## 数据库 Schema 的来源

`schema/schema.sql` **不是我们写的**——它是关系模式的唯一来源，由部长维护。

我们**只读，不手改**：`scripts/init_db.py` 直接执行这份 DDL 建库建表。
表结构确实要动，就在本仓库开分支改，PR 里写清理由，由 owner 审核。

**当前与 `bb38a50` 一致**（该提交同时是 `main` 的 HEAD）。

> 表名为什么叫 `member_position`：`position` 是 MySQL 保留字
> （`POSITION(x IN y)` 函数），裸写 `CREATE TABLE position` 会报
> `ER_PARSE_ERROR`（errno 1064）。owner 在 `bb38a50` 里把表改名，
> 并给全部标识符统一加了反引号。

## 技术栈

| 层 | 选型 |
|---|---|
| 语言 | Python 3.11+ |
| Web 框架 | FastAPI + Uvicorn |
| 数据库访问 | SQLAlchemy + PyMySQL |
| 结构化存储 | MySQL 8.0+（utf8mb4） |
| 向量检索 | Chroma + sentence-transformers（`BAAI/bge-small-zh-v1.5`，512 维，本地跑、不上云） |
| CLI | 标准库 `argparse` + `requests`（不依赖 `api/`，只发 HTTP） |
| 验收测试 | pytest |

> 早期有一版 Node/Express 的探索性骨架，技术栈定为 Python 后已删除。
> `server/`、`public/`、`package.json` 这些就是那一版留下的，随第一次提交一起清掉。

## 快速开始

组内成员请先看《任务分解与学习计划》（组内文档，不进仓库）——里面有逐步骤的环境配置说明、
每条线的任务分工和验收标准。下面是给已经配好环境的人的速查。

### 前置要求

| 项 | 要求 | 检查命令 |
|---|---|---|
| Python | ≥ 3.11 | `python -V` |
| MySQL | ≥ 8.0 | `mysql --version` |

### 装 MySQL

官网 https://dev.mysql.com/downloads/installer/ 下完整的 MSI（约 400MB，**不要**下 web 版）。

向导里这几屏注意：

1. Setup Type 选 **Server only**（别选 Developer Default，会装一堆用不上的东西）
2. Port 保持 **3306**
3. **设置 root 密码，自己记住**
   - ⚠️ 密码里**不要用 `#`**，`.env` 会把 `#` 之后的内容当注释吃掉
4. Windows Service 那一屏**两个勾都打上**（开机自启）
5. 想直接看表，装个 **MySQL Workbench**（和 Server 一起勾上就行）

### 起项目

```bash
git clone <这个仓库>
cd 知识库

python -m venv .venv
source .venv/Scripts/activate        # Windows Git Bash
# .venv\Scripts\activate             # Windows CMD / PowerShell
# source .venv/bin/activate          # Linux / macOS

pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

cp .env.example .env                 # 然后编辑 .env，填上你的 MySQL root 密码

python -m scripts.init_db            # 建库 + 建表（应建出 7 张表 2 个视图）
python -m scripts.seed               # 造演示数据（首次会下载向量模型，约 100MB）

python -m uvicorn api.main:app --reload --port 8000
```

打开 http://localhost:8000 就是演示页面，四个页签分别对应语义检索、经验录入、活动分工、报表统计。

> **每个人用的是自己电脑上的 MySQL**，数据库不共享、密码也不共享。
> 共享的是 `schema/schema.sql`——关系模式的唯一来源，谁都不手改。

> ⚠️ **首次下模型如果卡住不动**（注意：不是报错，是进度条不动），
> 是 HuggingFace 的 **xet 传输协议**在国内不通，不是网络断了。
> `api/config.py` 已经设了两个环境变量解决它：
> `HF_ENDPOINT=https://hf-mirror.com`（走国内镜像）+ `HF_HUB_DISABLE_XET=1`（关掉 xet）。
>
> **两个都得设，而且必须在 `huggingface_hub` 被导入之前设好**——
> 它在导入那一刻就把这两个值读成模块常量了，之后再设没有任何作用。
> 所以别在命令行临时 `export` 了事，要设在 `api/config.py` 这种一定会先被导入的地方。

## 常用命令

| 命令 | 作用 |
|---|---|
| `python -m uvicorn api.main:app --reload --port 8000` | 启动服务（改代码自动重启） |
| `python -m scripts.init_db` | 建库建表。**库已存在时会报错**（schema 不是幂等的） |
| `python -m scripts.reset_db` | ⚠️ **删库重建**，本地数据全没。会先问一句确认，加 `-y` 跳过 |
| `python -m scripts.seed` | 造演示数据。**可反复跑**，每次先清空再重建 |
| `python -m pytest tests/ -v` | 跑验收测试 |
| `python -m pipeline.fetch --kind member` | 数据管道：从在线表格拉数据 |
| `python -m pipeline.clean --kind member` | 数据管道：清洗并幂等入库 |

> 脚本一律用 `python -m scripts.xxx` 的形式跑，不要直接 `python scripts/xxx.py`
> ——后者会让 `from api import ...` 找不到路径。

## 命令行工具（CLI）

```
python -m cli <资源> <动作> [参数...]
```

给 QQ 机器人用，也方便人在终端里查东西。**服务得先起着**，CLI 自己不会把它拉起来。

| 命令 | 作用 |
|---|---|
| `cli health` | 自检：服务通不通、数据库通不通、向量库多少条 |
| `cli exp search "设备出故障了咋办"` | **语义检索**（机器人主要用这个） |
| `cli exp get exp-1` | 单条经验的元信息（作者名、活动名已翻好） |
| `cli exp list --limit 20` | 分页列出 |
| `cli exp add --content "..." --type 解答 --source "2026开甲学创部"` | 录入一条经验 |
| `cli exp delete exp-1` | 删除 |
| `cli member list --keyword 张` / `cli member get 1` / `cli member history 1` | 成员、履历（走脱敏视图） |
| `cli series list` / `cli activity list` | 活动系列 / 场次 |
| `cli part list --activity-id 1` | 参与记录 |
| `cli part add --activity-id 1 --member-id 2 --role 主讲 --hours 3.5` | 登记一条分工 |
| `cli report by-member` / `never-participated` / `rotation` | 三个统计报表 |

`python -m cli --help`，或任意子命令加 `--help`，都有完整说明。

### 输出

默认在 stdout 打**一行 JSON**，失败时也打，机器人不用去解析中文报错：

```json
{"ok": true, "code": "ok", "data": [...], "message": "...", "low_confidence": false}
```

加 `--pretty` 变成缩进版，给人看。

### 退出码

| 码 | 含义 | 机器人该怎么做 |
|---|---|---|
| 0 | 成功，**包括零结果** | 有结果就发；没结果就说「没找到相关经验」 |
| 2 | 参数写错了 | 是机器人自己的 bug，记日志 |
| 3 | 服务连不上 / 超时 | 「知识库服务暂时不可用」 |
| 4 | 404 找不到 | 「没有这条记录」 |
| 5 | 409 冲突 | **当作已经做过了**，算成功（重试安全） |
| 6 | 400 / 422 服务端拒绝 | 把 `message` 给用户看 |
| 7 | 服务在，但数据库不通 | 「数据库暂时不可用」 |
| 1 | 其它 | 通用报错 |

### 为什么机器人走 CLI 而不直连数据库

Chroma 1.5.9 的 `PersistentClient` 文档里写明了它「只适合本地开发和测试」，
两个进程同时开 `data/chroma/`，读的一方**不保证**能看到另一方写的数据。
服务是常驻的、CLI 是短命的——短命进程会读到旧索引，算出已经占用的 id，
然后**静默覆盖**一条经验。不报错，数据就没了。

所以规矩是：**同一时刻只许一个进程碰向量库**，服务当那个唯一进程，CLI 走 HTTP 找它。

附带的好处是校验逻辑零重复：409 重复登记、400 外键不存在、404，
服务端写好一次，CLI 全部继承。机器人写进去的数据和网页录入的走**同一套规则**。

### 两条刻意的限制

- **`part add` 没有 `--honor` 参数。** `/api/reports/by-member` 的荣誉次数是评优依据，
  而写接口没有鉴权。开放给群里的话，任何人都能给自己报一条荣誉，
  事后还看不出是谁报的。CLI 干脆不暴露这个参数。
- **`exp search` 默认按相似度过滤。** 服务端的检索**没有相似度下限**，
  库里只要有数据，问什么都返回 top_k 条。网页那边看得到相似度数字，人自己会判断；
  但机器人不能把凑数的结果当答案发出去。

  实测（种子数据，24 个问句）：相关问句 0.4920–0.7426，不相干问句 0.2598–0.5373。
  **两段是重叠的**，没有哪个数字能把两者切开——不相干里最高的 0.5373
  比相关里最低的 0.4920 还高。所以分两档：

  | 档 | 条件 | 行为 |
  |---|---|---|
  | ① | 低于 `--min-similarity`（默认 **0.45**） | 不返回任何结果，`code: "no_match"`，**退出码仍是 0** |
  | ② | 过了 ①，但最像的不到 **0.55** | 照常返回，但 `low_confidence: true`，机器人该把话说软 |

  第 ② 档是关键：那两条漏网的不相干问句（0.53 上下）会落进这一档，
  机器人说「可能相关」而不是「就是这条」。宁可放过、不可错杀——
  库里有答案却告诉用户「没找到」，比给一条用户自己看得出不太准的更糟。
  详细数字和理由写在 `cli/exp.py` 开头。

## 目录结构

```
知识库/
├── api/                        Web 层
│   ├── main.py                 入口 + lifespan（在这里加载向量模型）
│   ├── config.py               读 .env
│   ├── db.py                   MySQL 连接池
│   ├── models.py               ORM 映射（7 表 2 视图）—— 共享，改动需对齐
│   ├── schemas.py              接口契约 —— 共享
│   ├── vectors.py              向量库 + embedding（三个坑都记在里面）
│   └── routers/
│       ├── experience.py       K1 录入 / K2 检索 / K3 元信息
│       ├── directory.py        K4 成员 / 活动 / 参与记录
│       └── reports.py          K5 报表与轮值
├── cli/                        命令行工具（QQ 机器人从这里读写）
│   ├── __main__.py             让 `python -m cli` 能跑
│   ├── main.py                 命令注册、输出信封、退出码出口
│   ├── client.py               HTTP 封装。★ 为什么走 HTTP 不直连，看这个文件开头
│   ├── exp.py                  经验：search / get / list / add / delete
│   ├── directory.py            成员 / 系列 / 活动 / 参与记录
│   └── report.py               三个统计报表
├── pipeline/                   数据管道线
│   ├── contract.py             中间格式约定 —— 两人共同确定
│   ├── fetch.py                从在线表格拉取
│   └── clean.py                清洗 + 幂等入库
├── scripts/                    运维脚本
│   ├── init_db.py              建库建表
│   ├── reset_db.py             删库重建（危险）
│   ├── seed.py                 演示假数据
│   └── console.py              Windows 控制台编码兜底
├── tests/                      验收测试（每条线一个文件）
├── deploy/                     部署
│   ├── scitech-kb.service      systemd 单元
│   └── 部署说明.md              服务器上怎么跑起来
├── web/index.html              演示前端（正式前端是二期）
├── 技术栈可行性/                 写代码之前先证明这条路能走通（结论：成立）
├── requirements.txt            依赖清单
├── docs/                       文档
│   ├── 开发说明.md              本文件
│   ├── 需求文档.md              部长写的需求（what/why）
│   └── 交接文档.md              数据源、初始化、部署、待办（how）
├── schema/schema.sql           数据库图纸（唯一来源，勿手改）
├── CONTRIBUTING.md             协作规范：红线 + PR 流程
├── .env.example                配置模板（不含真实值，可以提交）
└── .env                        你的真实配置（已被 .gitignore 忽略）
```

## 接口

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/health` | 一次看三件事：数据库通不通、向量库多少条、模型是什么 |
| POST | `/api/experience` | K1 录入经验 |
| GET | `/api/experience/search?q=` | K2 **语义检索**（核心） |
| GET | `/api/experience/keyword?q=` | 关键词字面匹配（只用来和语义检索做对照） |
| GET | `/api/experience/{exp_id}` | K3 经验元信息（把 author_id 翻成人名） |
| GET | `/api/members?keyword=` | 成员列表（走 `v_member_public` 视图，**自动脱敏**） |
| GET | `/api/members/{id}` | 成员详情（同样走视图） |
| GET | `/api/members/{id}/history` | 个人活动履历 |
| POST | `/api/members` | 新增成员 |
| GET/POST | `/api/series` | 活动系列 |
| GET/POST | `/api/activities` | 活动场次 |
| GET/POST | `/api/participations` | 参与记录（重复登记返回 409） |
| GET | `/api/reports/by-member` | 按人聚合工作量 |
| GET | `/api/reports/never-participated` | 谁还没参与过 |
| GET | `/api/reports/rotation` | 按学期统计参与分布 |

服务起来后，http://localhost:8000/docs 有 FastAPI 自带的接口文档，可以直接在页面上点着试。

## 数据库结构

7 张表 + 2 个视图。核心关系：

```
activity_series  1 ──N  activity  N ──N  member      （经 participation）
member           1 ──N  member_position
member           1 ──1  account / privacy_consent
```

| 表 | 作用 |
|---|---|
| `member` | 成员档案（内外分层，敏感字段有标注） |
| `activity_series` | 活动系列（品牌/栏目，如「惟学沙龙」） |
| `activity` | 活动场次（系列下具体一次） |
| `participation` | 参与记录：谁 × 哪个活动 × 什么分工 |
| `member_position` | 职务/任期（支持多职务、换届）。⚠️ **表名是 `member_position` 不是 `position`**——后者是 MySQL 关键字 |
| `privacy_consent` | 隐私授权记录 |
| `account` | 账号（学校统一身份 CAS 绑定） |

| 视图 | 作用 |
|---|---|
| `v_member_public` | 对外简版成员卡，**自动脱敏**（不含学号/手机/邮箱/班级/生日） |
| `v_member_history` | 成员活动履历，自动 JOIN 聚合，不用手填 |

### 双库怎么关联

经验本体存在**向量库**里，MySQL 里**没有 experience 表**。两边靠 id 关联：

- `participation.exp_ids`（JSON 列）存 `exp-xxx`，正向指向经验；
- 经验的 metadata 里记 `author_id` / `activity_id` / `participation_id`，反向指回 MySQL。

这几个 id 和 MySQL 的 `member.id` / `activity.id` / `participation.id` 是**同一套**。

### 三个设计要点

- **`participation` 的唯一键是 `(activity_id, member_id, role)`**，不是 `(活动, 人)`。
  这意味着**一个人在一个活动里可以有多个角色**（既当主讲又当执行）。
- **`v_member_public` 视图就是脱敏规则的唯一事实来源**。
  写查询时直接 `SELECT * FROM v_member_public`，不要在代码里再抄一份字段清单——
  抄了就会有两处规则要维护，视图改了代码没改就报 `Unknown column`。
- **向量距离空间必须是 cosine**。Chroma 不指定时用 L2 且返回**平方**距离，
  按「1 − 距离」算出来的不是相似度（真值 0.565 会被算成 0.130），
  而且**不报错**，静默给错数字。详见 `api/vectors.py` 开头那三个坑。

## 待定事项

- [x] ~~技术栈方向未定~~ → 已定：Python + FastAPI。原来的 Node/Express 探索性骨架已删除。
- [x] ~~代码放哪~~ → 已定：**代码就放在本仓库**（`CONTRIBUTING.md` 里
      「代码：录入/查询脚本、工具」属于可提交内容）。走 `feature/` 分支 + PR，
      owner 审核后 merge。collaborator 权限已开。
- [ ] **CAS 统一身份认证：一期不做，列为后续计划。**
      需求文档 §4.3 要求「复用学校统一身份认证（CAS），不自建密码」。
      **一期的服务不实现登录，也不做鉴权。** 三个原因：
      1. 需求文档自己的「待确认」表里就写着 CAS 对接方式**待评估**，并注明
         「服务器在内网，CAS 回调可能需内网穿透或代理」——**这不是我们能单方面定的**，
         得先跟服务器管理员确认回调怎么走。
      2. 它服务的是 K11 权限分级，而 K11 在需求文档里是 **P2（可选）**。
      3. 服务只监听 `127.0.0.1`，本来就只在服务器本机用，没有对外暴露。

      `account` 表结构已经在 `schema/schema.sql` 里（由部长维护），
      **将来接 CAS 不需要改表、不返工**。
      在 CAS 落地之前，**访问控制放在部署层**（只绑回环地址，见 `deploy/部署说明.md`），
      应用层暂时没有身份概念——这也是「机器人写接口没有鉴权」那条待办的同一个根因。

- [ ] **数据字典还没有**。需求文档的验收标准里有一条「有一份数据字典 + 使用说明」，
      目前只满足了一半（使用说明有，数据字典没有）。
      数据字典要写清每个字段的含义、是否敏感、谁能看——**权限模型得先定下来**，
      这是要组长和部里确认的事，不是技术问题。
- [ ] 上线前要收紧 CORS。现在 `api/main.py` 里是 `allow_origins=["*"]`，
      任何网站都能调这套接口，包括读成员信息。
- [ ] **机器人的写权限是个政策问题，不是技术问题。**
      写接口（`POST /api/experience`、`POST /api/participations`）目前没有任何鉴权。
      CLI 这一侧已经把 `--honor` 拿掉了，但「谁能在群里让机器人登记分工」
      得组长和部里定——最省事的做法是机器人组维护一个 QQ 号白名单。
- [x] ~~`position` 保留字的修复~~ → 已反馈给 owner，`bb38a50` 已修（改名 `member_position`）
