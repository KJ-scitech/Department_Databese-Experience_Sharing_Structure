# ============================================================
# 学术科创部知识库 · .gitignore
# 原则：框架（schema / 代码 / 文档 / 配置模板）可上传；
#       具体数据、敏感信息、密钥一律禁止上传。
#
# 本文件以 owner 的版本为底，技术组补了几条（标了 ★），合并而来。
# ============================================================

# ---------- 1. 数据（完全禁止）----------
# 数据库文件
*.db
*.sqlite
*.sqlite3
*.mdb
*.accdb

# 数据导出 / 备份（csv / excel / tsv / dump）
*.csv
*.tsv
*.xlsx
*.xls
*.ods
*export*
*backup*
*导出*
*备份*
*dump*
# ★ 数据库快照（旧名，可能还留在某些人的目录里）
*快照*
sql_dump/

# SQL：只允许 schema/ 下的结构定义，其他一律排除
*.sql
!schema/*.sql

# 数据目录
data/
**/data/
!schema/

# ---------- 2. 成员 / 个人信息 ----------
*成员*
*名单*
*通讯录*
*信息表*
*报名*
*签到*
# ★ 组内几个含真实姓名的文件，逐条点名锁上。
#   它们不在仓库里，但不在 .gitignore 里就等于没上锁——
#   谁哪天顺手一个 `git add -A` 就把真名推上去了。想给人看就发群里或 SeaTable。
组员.txt
分工.md
任务拆解.md
# ★ 部长发的需求文档原稿：含服务器 IP、内网架构、端口号、密钥存放路径。
#   整理版已经在 docs/需求文档.md，这份原稿只在本机留底。
学创部技术组需求文档*.docx
# 这里**故意不写 `*.docx`**：全局忽略会把以后任何需要入库的 Word 文档也一起挡掉，
# 而且 git 不报错，文件只是悄悄没上去，很难发现。要锁哪份就照上一行逐条点名。

# ---------- 3. 密钥 / 凭证 ----------
.env
*.env
.env.*
# ★ 上面这条 .env.* 会把 .env.example（配置模板，不含真实值）一起罩住，
#   而 CONTRIBUTING 明确允许提交它。反排除必须写在忽略规则后面，顺序不能调。
!.env.example
secrets/
.secure/
*.key
*.pem
*.p12
*.pfx
*token*
*secret*
*credential*
*.secret

# ---------- 4. 系统 / 编辑器 ----------
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# ---------- 5. 运行时 ----------
__pycache__/
*.pyc
*.log
.venv/
venv/
node_modules/
# ★ 下面三条是 Python 侧的运行时产物，各自下载/生成，不进仓库
.pytest_cache/
models/
.cache/
